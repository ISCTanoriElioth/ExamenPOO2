public class Hiloprincipal extends Thread{//Extendemos funcion Thread.
public void corre() { //Recorremos las posiciones.
        for (int i = 0; i < i; i++) {//Incremental.
            if (i %2 == 0) {//indice porcentaje de dos, sea igual a 0.
            }
        }
    }
}